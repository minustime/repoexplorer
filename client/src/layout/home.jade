.container-fluid
    .row
        .col-sm-4
        .col-sm-4
            section
                .search.main(ng-controller='Search as vm')
                    form(ng-submit="vm.getProfile()")
                        .form-group
                            label Enter a Github organization
                            input.form-control(type="text", name="org-search", placeholder="Organization name", required ng-model="vm.search.term")
                        .form-group
                            button.btn.btn-default.btn-lg.btn-block(type="submit") Explore!
        .col-sm-4